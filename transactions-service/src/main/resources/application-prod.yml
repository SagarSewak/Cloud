server:
  port: 8002

spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
  flyway:
    enabled: true

logging:
  level:
    root: INFO
    com.gdb.transactions: INFO
